<div class="row">

    <div class="col-md-12">
        {{ form_start(form) }}
        {{ form_row(form.name) }}

        <button class="btn btn-primary btn-block ">{{ button|default('Enregistrer') }}</button>

    </div>


    {{ form_end(form) }}
</div>
